apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: hello-world-test
spec:
  params:
    - default: 'https://github.com/cmays20/hello-world'
      name: GIT_REPO
      type: string
    - default: master
      name: GIT_REVISION
      type: string
    - default: 'registry.access.redhat.com/ubi9/openjdk-17:latest'
      name: MAVEN_IMAGE
      type: string
    - name: REPO_FULL_NAME
      type: string
    - name: SHA
      type: string
  tasks:
    - name: get-route-to-console
      params:
        - name: SCRIPT
          value: "oc get route console -n openshift-console --template='{{ .spec.host }}'"
      taskRef:
        kind: Task
        name: oc-with-results
    - name: github-set-status-pending
      params:
        - name: REPO_FULL_NAME
          value: $(params.REPO_FULL_NAME)
        - name: SHA
          value: $(params.SHA)
        - name: DESCRIPTION
          value: Build has started
        - name: STATE
          value: pending
        - name: TARGET_URL
          value: >-
            https://$(tasks.get-route-to-console.results.oc-output)/k8s/ns/$(context.pipelineRun.namespace)/tekton.dev~v1~PipelineRun/$(context.pipelineRun.name)/logs
      taskRef:
        resolver: hub
        params:
          - name: catalog
            value: tekton-catalog-tasks
          - name: type
            value: artifact
          - name: kind
            value: task
          - name: name
            value: github-set-status
          - name: version
            value: "0.4"
      when:
        - input: $(params.SHA)
          operator: notin
          values:
            - TEST
      runAfter:
        - get-route-to-console
    - name: fetch-repository
      params:
        - name: url
          value: $(params.GIT_REPO)
        - name: revision
          value: $(params.GIT_REVISION)
        - name: deleteExisting
          value: 'true'
      taskRef:
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: name
            value: git-clone
          - name: namespace
            value: openshift-pipelines
      workspaces:
        - name: output
          workspace: workspace
    - name: maven-build
      params:
        - name: MAVEN_IMAGE
          value: $(params.MAVEN_IMAGE)
        - name: GOALS
          value:
            - test
      runAfter:
        - fetch-repository
      taskRef:
        kind: Task
        name: maven-with-cache
        apiVersion: tekton.dev/v1
      workspaces:
        - name: source
          workspace: workspace
        - name: maven-repo
          workspace: maven-repo
        - name: maven-settings
          workspace: maven-settings
  finally:
    - name: report-results-error
      params:
        - name: REPO_FULL_NAME
          value: $(params.REPO_FULL_NAME)
        - name: SHA
          value: $(params.SHA)
        - name: DESCRIPTION
          value: Build has started
        - name: STATE
          value: error
        - name: TARGET_URL
          value: >-
            https://$(tasks.get-route-to-console.results.oc-output)/k8s/ns/$(context.pipelineRun.namespace)/tekton.dev~v1~PipelineRun/$(context.pipelineRun.name)/logs
      taskRef:
        resolver: hub
        params:
          - name: catalog
            value: tekton-catalog-tasks
          - name: type
            value: artifact
          - name: kind
            value: task
          - name: name
            value: github-set-status
          - name: version
            value: "0.4"
      when:
        - input: $(tasks.maven-build.status)
          operator: in
          values:
            - Failed
        - input: $(params.SHA)
          operator: notin
          values:
            - TEST
    - name: report-results-success
      params:
        - name: REPO_FULL_NAME
          value: $(params.REPO_FULL_NAME)
        - name: SHA
          value: $(params.SHA)
        - name: DESCRIPTION
          value: Build has started
        - name: STATE
          value: success
        - name: TARGET_URL
          value: >-
            https://$(tasks.get-route-to-console.results.oc-output)/k8s/ns/$(context.pipelineRun.namespace)/tekton.dev~v1~PipelineRun/$(context.pipelineRun.name)/logs
      taskRef:
        resolver: hub
        params:
          - name: catalog
            value: tekton-catalog-tasks
          - name: type
            value: artifact
          - name: kind
            value: task
          - name: name
            value: github-set-status
          - name: version
            value: "0.4"
      when:
        - input: $(tasks.maven-build.status)
          operator: in
          values:
            - Succeeded
        - input: $(params.SHA)
          operator: notin
          values:
            - TEST
  workspaces:
    - name: workspace
    - name: maven-repo
    - name: maven-settings
